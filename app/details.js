import { Text } from "react-native";
import { StyledContainer, InnerContainer } from "../src/components/styled";

export default function DetailsScreen() {
  return (
    <StyledContainer>
      <InnerContainer>
        <Text>Details screen.</Text>
      </InnerContainer>
    </StyledContainer>
  );
}
