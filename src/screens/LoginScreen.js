import React from "react";
import {
  StyleContainer,
  InnerContainer,
  PageTitle,
} from "../components/styled";

const LoginScreen = () => {
  <StyleContainer>
    <InnerContainer>
      <PageTitle>Login</PageTitle>
    </InnerContainer>
  </StyleContainer>;
};

export default LoginScreen;
